# Landing Page Source

This folder contains the separate React/Vite source for the public Nourishly project landing page.

## Local preview

```bash
npm install
npm run dev
```

## Build for GitHub Pages

```bash
npm run build
```

The build output is written to the repository-level `docs/` folder, which is the folder intended for GitHub Pages publishing.
